{
  "name": "actors-platform",
  "version": "1.0.0",
  "description": "Actor-Agent Development Platform",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*",
    "actors/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "deploy": "turbo run deploy",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0",
    "@testing-library/react": "^14.0.0",
    "happy-dom": "^12.0.0",
    "vitest": "^0.34.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}